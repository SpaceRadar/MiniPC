fit_loadaddr=0x10000000
fit_file=fit.itb
serverip=192.168.0.100
bootargs=earlycon=uart8250,mmio32,0xff1a0000
sdboot=fatload mmc 1:3 ${fit_loadaddr} ${fit_file} && bootm ${fit_loadaddr}
netboot=dhcp ${fit_loadaddr} ${fit_file} && bootm ${fit_loadaddr}
modeboot=sdboot
bootcmd=run ${modeboot}
