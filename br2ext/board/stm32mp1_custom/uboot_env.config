fit_loadaddr=0xc6000000
fit_file=fit.itb
serverip=192.168.0.100
mmcdev=0:4

sdboot=fatload mmc ${mmcdev} ${fit_loadaddr} ${fit_file} && bootm ${fit_loadaddr}
netboot=dhcp ${fit_loadaddr} ${fit_file} && bootm ${fit_loadaddr}
modeboot=sdboot
bootcmd=run ${modeboot}
bootcmd_distro=run ${modeboot}
